<div class="row">
  <div class="large-12 medium-12 small-12 columns">
    <% if notice %>
      <div data-alert class="alert-box">
        <%= notice %>
        <a href="#" class="close">&times;</a>
      </div>
    <% end %>
  
    <h1><%= @contact.full_name %></h1>
    <h2><%= @contact.company_name %></h2>
  
    <% if @contact.member_since > Time.now %>
      <div data-alert class="alert-box info">
        This member is currently awaiting membership approval.
      </div>
    <% end %>
  
    <p>
      <%= member_category %><br>
      Member since <%= @contact.member_since.strftime('%D') %>
    </p>
  </div>
</div>
  
<div class="row">
  <div class="large-6 medium-6 small-12 columns">
    <h3>Mailing Address</h3>
    <p>
      <%= @contact.address1 %><br>
      <% unless @contact.address2.empty? %>
      <%= @contact.address2 %><br>
      <% end %>
      <%= @contact.city %>, <%= @contact.state %> <%= @contact.zip_code %>
    </p>
  </div>
  <div class="large-6 medium-6 small-12 columns">
    <h3>Telephone</h3>
    <p><%= link_to @contact.phone, "tel:#{@contact.phone.gsub(/\W/,'')}" %></p>
  </div>
</div>
  
<div class="row">
  <div class="large-12 medium-12 small-12 columns">
    <h3>Event Attendance</h3>
    <table>
      <thead>
        <tr>
          <th>Event</th>
          <th>Date</th>
          <th>Location</th>
          <th>Distance</th>
          <th>Attended?</th>
        </tr>
      </thead>
      <tbody>
      </tbody>
    </table>
  
    <%= link_to 'Edit', edit_contact_path(@contact) %> |
    <%= link_to 'Back', contacts_path %>
  </div>
</div>
